{% extends "service/layout.html.twig" %}

{% block main %}
    <h2 class="text-center mb-5 text-info">Nauja PASLAUGA</h2>
    <div class="container">
        <div class="row">
            <div class="col-sm-8">
                {#{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}#}
                {{ form_start(serviceForm, {'method': 'post', 'action': path('serviceAdd'), 'attr': {'class': 'fos_user_registration_register'}}) }}
                {{ form_widget(serviceForm) }}
                <div class="row mt-5">
                    <div class="col">
                        <input type="submit" class="btn btn-info col-sm-2 offset-4" value="Išsaugoti" />
                    </div>
                </div>

                {{ form_end(serviceForm) }}
             </div>
            <div class="col-sm-4">
                {% include '/_map/map_divs.html.twig' %}
            </div>
        </div>
    </div>

{% endblock %}

{% block javascripts  %}
    {{ parent() }}
    {% include '/_map/map_js.html.twig' %}
{% endblock %}