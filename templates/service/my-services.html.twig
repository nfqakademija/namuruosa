{% extends 'layout/layout-fluid-container.html.twig' %}

{% block body %}
    <div class="row">
        <div class="serviceWrapper container-under-fixednav">
            <div class="page-title-elipse">
                <h2>MANO PASLAUGOS</h2>
            </div>

            {% if (services != []) %}
            {% include 'service/table/services.html.twig' %}
                {% else %}
                    <div class="container">
                        <h3 class="text-danger">Jūs neturite užregitstruotų paslaugų</h3>
                        <div class="row">
                            <div class="col mt-3">
                                <a href="{{ path('serviceAdd') }}"><button class="btn btn-link"><i class="fas fa-pencil-alt"></i> Sukurti Naują Paslaugą</button></a>
                            </div>
                        </div>
                    </div>
            {% endif %}
        </div>
    </div>

{% endblock %}