<h3>Jūsų užklausą atitinkanti paslauga</h3>
<p>Jeigu norite susisiekti su paslaugos autorium - spauskite MATCH</p>

<table class="table">
    <tr>
        <th>Vardas</th>
        <th>Aprašymas</th>
        <th>Laikas nuo</th>
        <th>Laikas iki</th>
        <th>Kategorijos</th>
        <th>Koordinatės</th>
        <th></th>
    </tr>
        <tr>
            <td><a target="_blank" href="{{ path('profile', {'id': id}) }}">{{ username }}</a></td>
            <td>{{ service.description }}</td>
            <td>{{ service.activeTimeStart|date('H:i:s') }}</td>
            <td>{{ service.activeTimeEnd|date('H:i:s') }}</td>
            <td>
                {%  if service.transport %}
                    Transportas,
                {% endif %}
                {%  if service.education %}
                    Mokymai,
                {% endif %}
                {%  if service.education %}
                    Valymo paslaugos,
                {% endif %}
            </td>
            <td>
                X: {{ service.coordinateX }}, Y: {{ service.coordinateY }}
            </td>
            <td>
                <button class="btn btn-sm btn-success">MATCH</button>
            </td>
        </tr>

</table>
