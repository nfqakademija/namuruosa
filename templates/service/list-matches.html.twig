{% extends 'service/layout.html.twig' %}

{% block body %}

    {% for matchesByJob in matchesByJobs %}
        {%  if matchesByJob != null %}
            {% include 'service/table/listTable-matches.html.twig' %}
        {% else %}
            <h5 class="text-danger">Šiuo metu turime pasiulymu </h5>
            <hr>
        {% endif %}
    {% endfor %}


{% endblock %}