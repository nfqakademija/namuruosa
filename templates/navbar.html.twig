<nav class="navbar navbar-expand-lg navbar-dark bg-dark text-nowrap">
    <a class="navbar-brand" href="/"><i class="fa fa-hand-peace fa-2x mr-2"></i> Namų Ruoša</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <ul class="collapse navbar-collapse" id="navbarSupportedContent">

        {% if is_granted('ROLE_ADMIN') %}

            {% include 'navbar/admin.html.twig' %}

        {% endif %}

        <div class="ml-auto"></div>

        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}

            {% include 'navbar/remembered.html.twig' %}

        {% else %}

            {% include 'navbar/anonymous.html.twig' %}

        {% endif %}

    </ul>

</nav>